default: &default
  adapter: postgresql
  username: friend
  password: lighthouse
  host: localhost

development:
  <<: *default
  database: find_me_friends

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: find_me_friends_test

production:
  <<: *default
  database: find_me_friends_production
