<%= link_to 'Back', admin_root_url %>
<div>
  <h1> There are currently <%= @users.count %> users finding frands.</h1>
</div>
  <table>
    <tr>
      <th>User ID</th>
      <th>Name</th>
    </tr>
<% @users.each do |user| %>
    <tr>
      <td><%= user.id %></td>
      <td><%= user.first_name %> <%= user.last_name %></td>
      <td>Delete currently doesn't work
<% end %>
  </table>