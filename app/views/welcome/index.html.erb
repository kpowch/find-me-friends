<div class='welcome-page'>
  <div class='fullscreen-bg'>
    <%= video_tag "rollercoaster.mp4", autoplay: :autoplay, loop: :loop, mute: :mute, class: "video" %>
  </div>
  <div class='welcome-message'>
    <div class='flash-message'>
      <% if flash[:alert] %>
        <%= flash[:alert] %>
      <% end %>
    </div><br/><br/><br/>
    <div class='welcome-screen'>
      <h1 class='welcome-header'>Welcome to Find Me Friends</h1>
      <p class='welcome-blurb'>Let us be the <strong>mutual friend</strong> to introduce you to your new best <br/>friend. Sign up now to start making new amazing friends.</p>
    </div>
  </div>
  <br/><br/><br/>
  <% if current_user.nil? %>
    <main class='welcome-login'>
      <input id='tab1' class='signup-tab' type='radio' name='tabs' checked>
      <label for='tab1' class='welcome'>Login</label>
      <input id='tab2' class='signup-tab' type='radio' name='tabs'>
      <label for='tab2' class='welcome'>Sign Up</label>

      <section class='welcome-tabs' id='content1'>
        <div>
          <%= form_tag '/login' do %>
            <span class='form-field-input'> <%= email_field_tag(:email, '', placeholder: 'Email') %></span>
            <span class='form-field-input'> <%= password_field_tag(:password, '', placeholder: 'Password') %></span>
            <%= submit_tag 'Submit', class: 'form-button' %>
          <% end %>
        </div>
      </section>

      <section class='welcome-tabs' id='content2'>
        <div>
          <%= form_for :user, url: '/users' do |f| %>
            <span class='form-field-input'> <%= f.text_field :first_name, placeholder: 'First Name'%></span>
            <span class='form-field-input'> <%= f.text_field :last_name, placeholder: 'Last Name' %></span>
            <span class='form-field-imput'> <% f.label :dob %> Date of Birth: <br/><br/> <%= f.date_select :dob, order: [:month, :day, :year], prompt: { day: 'Select day', month: 'Select month', year: 'Select year' }, start_year: Date.today.year - 18, end_year: Date.today.year - 80 %> </span>
            <span class='form-field-input'> <%= f.email_field :email, placeholder: 'Email' %></span>
            <span class='form-field-input'> <%= f.password_field :password, placeholder: 'Password'%></span>
            <span class='form-field-input'> <%= f.password_field :password_confirmation, placeholder: 'Password Confirmation' %></span>
            <%= f.submit 'Submit', class: 'form-button' %>
          <% end %>
        </div>
      </section>
    </main>
  <% end %>
</div>
